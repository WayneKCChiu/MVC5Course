@if (User.Identity.IsAuthenticated) {
   <ul class="nav navbar-nav navbar-right">
      <li>
         @User.Identity.Name
      </li>
      <li>@Html.ActionLink("登出", "LoginOut", "Member")</li>
   </ul>
}
else {
   <ul class="nav navbar-nav navbar-right">
      <li>@Html.ActionLink("登入", "Login", "Member")</li>
   </ul>
}
